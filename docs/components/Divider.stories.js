import { } from "./Divider";

export default {
    title: 'Divider',
    argTypes: { },
    parameters: {
        docs: {
            page: null
        }
    },
}

const Template = ({ ...args }) => {
    // return createDiv(args);
    return '';
}

export const Divider = Template.bind({});
